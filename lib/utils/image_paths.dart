/// A utility class that holds the paths for image assets used in the app.
class ImagePaths {
  /// Base path for all image assets.
  static const String basePath = 'assets/images';

  /// Path to the splash background image.
  static const String splashBackground = '$basePath/splash_bg.png';
}
